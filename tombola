#include <iostream>
#include <cstdlib>
#include <ctime>

using namespace std;

bool usciti[91] = {false}; // indice 1–90


int stampa() {
    

    cout << "TOMBOLA"<<endl;

    for (int i = 1; i <= 90; i++) {
        if (usciti[i]) {
    cout << "|  |";  
	}	
		else 
		{    if(i<10)
		    cout << "|"<< i << " |";
		    else
            cout << "|"<< i << "|";
            
        }

        if (i % 10 == 0)
            cout << endl;
    }
    cout << endl;
}
